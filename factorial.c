#include<stdio.h>

int main(){

    int mian()
    {
        int a,n,factorial;
        printf("enter number to find the factorail");
        dcanf("%d",n);
        for(i=0;i<n;i++)
        {
            factorial*=i;
        }
        printf("this is the value of facroral %d",factorial);
    }
    return 0;
}